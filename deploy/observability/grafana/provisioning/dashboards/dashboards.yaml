apiVersion: 1
providers:
  - name: Keyloop Unified Documents
    folder: Keyloop
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards